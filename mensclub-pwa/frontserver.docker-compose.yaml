services:
  frontend:
    container_name: frontend
    build: 
      context: .
      dockerfile: Dockerfile.React
    ports:
      - "3001:3000"
    networks:
      - elk-networks 

networks:
  elk-networks:
    external: True
  